# disruptor-rs

Rust implementation of LMAX Disruptor pattern.

## Quick Start

Add to `Cargo.toml`:

```toml
[dependencies]
disruptor-rs = { git = "https://github.com/UnprofessionalCoder/disruptor-rs" }
```
